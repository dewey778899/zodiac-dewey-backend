import type { FeatureCollection, Geometry, GeometryCollection } from '@turf/helpers';
import type { ChildrenDataOptions, IDataOptions, ISourceOptions } from './baseSource';
import BaseSource from './baseSource';
export declare class DataVSource extends BaseSource {
    info: {
        desc: {
            text: string;
            href: string;
        };
        url: string;
    };
    protected getDefaultOptions(): Partial<ISourceOptions>;
    getRenderData(options: Partial<IDataOptions>): Promise<FeatureCollection<Geometry | GeometryCollection, Record<string, any>>>;
    getData(options: Partial<IDataOptions>): Promise<FeatureCollection<Geometry | GeometryCollection, Record<string, any>>>;
    getChildrenData(ChildrenDataOptions: Partial<ChildrenDataOptions>): Promise<FeatureCollection<Geometry | GeometryCollection, Record<string, any>>>;
    fetchData(code: number, full?: boolean): Promise<FeatureCollection<Geometry | GeometryCollection, Record<string, any>>>;
}
